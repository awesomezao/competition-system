import React, { ReactElement } from 'react'

interface Props {
  
}

function Awesome({}: Props): ReactElement {
  return (
    <div>
      awesome
    </div>
  )
}

export default Awesome
