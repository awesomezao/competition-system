import React, { ReactElement } from 'react'

interface Props {
  
}

function index({}: Props): ReactElement {
  return (
    <div>
      message
    </div>
  )
}

export default index
